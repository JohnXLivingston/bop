/* eslint-disable no-unused-expressions */
import { describe, before, after, it } from 'mocha'

import { flushTests } from '../test-utils'

describe('models/user/user.ts', function () {
  before(flushTests)
  after(flushTests)

  it('TODO: test user model')
})
